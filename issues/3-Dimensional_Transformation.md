现状：在encoder.py的实现中，需要将roberta模型的输出维度进行调整，以适应下游任务的需求。具体来说，roberta-base模型的输出维度为768，而某些任务可能需要不同的维度（例如512）。因此，我们需要添加一个线性变换层，将768维的输出映射到所需的维度。
问题：这样的实现会导致训练有点慢
解决：
添加一种方式，使得用户可以选择是否进行这种线性变换。如果用户指定了一个不同于768的输出维度，我们就添加这个线性层；否则，我们直接使用roberta的输出。
